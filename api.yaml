villager:
  countries:
    get:
      description: >
        Fetch a single country by id, alpha2, alpha3 or numeric codes
      returns: Country | None
    filter:
      description: >
        Fetch a list of countries filtered by field. Can also filter by all fields with a general query. 
      returns: list[Country]
    # search:
    #   description: >
    #     Search for a country with fuzzy matching and returning a list of candidates.
    #   returns: list[tuple[Country, float]]

  subdivisions:
    get:
      description: >
        Fetch a single subdivision by unique id, ISO 3166-2 code or geonames code.
      returns: Subdivision | None
    filter:
      description: >
        Fetch a list of subdivisions filtered by field. Can also filter by all fields with a general query. 
      returns: list[Subdivision]
    # search:
    #   description: >
    #     Search for a subdivisions with fuzzy matching and returning a list of candidates.
    #   returns: List[Tuple[Subdivision, float]]
    # for_country:
    #   description: >
    #     Fetch all subdivisions for a given country by code.
    #   returns: List[Subdivision]
    # types:
    #   description: >
    #     Fetch distinct subdivision types for a given country (e.g. "state", "province"). Can filter by admin level to refine the list. Helpful for dynamic dropdowns.
    #   returns: List[str]

  cities:
    get:
      description: >
        Fetch a single city by unique id or geonames code.
      returns: City | None
    filter:
      description: >
        Fetch a list of subdivisions filtered by field. Can also filter by all fields with a general query. 
      returns: list[City]
  #   search:
  #     description: >
  #       Search for a city with fuzzy matching and returning a list of candidates..
  #     returns: List[Tuple[City, float]]
  #   for_country:
  #     description: >
  #       Return all cities for a given country name/code (alpha-2/3).
  #     returns: List[City]
  #   for_subdivision:
  #     description: >
  #       Return all cities for a given subdivision code.
  #     returns: List[City]
  # autocomplete:
  #   description: >
  #     Autocomplete for all cities, subdivisions and countries.
